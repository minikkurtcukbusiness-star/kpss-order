/* Lesson richness layer: ensures the third step is a real worked-example prompt, not the same generic paragraph for every topic. */
(function(){if(typeof DERS_OGRETIM==='undefined')return;Object.keys(DERS_OGRETIM).forEach(k=>{const item=DERS_OGRETIM[k];if(!item?.adimlar||item.adimlar.length<3)return;const base=item.adimlar[1]?.metin||item.tanim||'';const example=item.adimlar[2];example.baslik='Uygulamalı örnek';example.metin=`${k} için şimdi öğrendiğin kuralı uygulayalım. Önce verilen bilgileri yaz, sonra şu adımları sırayla uygula: 1) ilgili kavramı belirle, 2) kuralı seç, 3) işlemi/çıkarımı yap, 4) sonucu sorunun istediği biçimde kontrol et. ${base}`;example.ipucu='Önce yöntemi söyle, sonra işlemi yap; cevabı en sonda seçeneklerle karşılaştır.';example.kontrol=`Kendin dene: ${k} konusunda öğrendiğin kuralla bir örnek oluştur ve sonucunu açıklamadan önce hangi adımı neden yaptığını söyle.`;});})();
